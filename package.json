{
  "name": "jobtrail-application-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "本地优先的求职投递进度管理看板",
  "scripts": {
    "start": "node scripts/dev-server.mjs",
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/model.js && node --check src/storage.js && node --check src/feishu-sync.js && node --check server/feishu-sync/server.js && node --check server/feishu-sync/service.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
